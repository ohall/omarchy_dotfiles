# Application bindings
bindd = SUPER, RETURN, Terminal, exec, uwsm-app -- xdg-terminal-exec --dir="$(omarchy-cmd-terminal-cwd)"
bindd = SUPER SHIFT, F, File manager, exec, uwsm-app -- nautilus --new-window
bindd = SUPER SHIFT, B, Browser, exec, omarchy-launch-browser
bindd = SUPER SHIFT ALT, B, Browser (private), exec, omarchy-launch-browser --private
bindd = SUPER SHIFT, M, Music, exec, omarchy-launch-or-focus spotify
bindd = SUPER SHIFT, N, Editor, exec, omarchy-launch-editor
bindd = SUPER SHIFT, D, Docker, exec, omarchy-launch-tui lazydocker
bindd = SUPER SHIFT, G, Signal, exec, omarchy-launch-or-focus ^signal$ "uwsm-app -- signal-desktop"
bindd = SUPER SHIFT, O, Obsidian, exec, omarchy-launch-or-focus ^obsidian$ "uwsm-app -- obsidian -disable-gpu --enable-wayland-ime"
bindd = SUPER SHIFT, W, Typora, exec, uwsm-app -- typora --enable-wayland-ime
bindd = SUPER SHIFT, SLASH, Passwords, exec, uwsm-app -- 1password

# If your web app url contains #, type it as ## to prevent hyprland treating it as a comment
bindd = SUPER SHIFT, A, ChatGPT, exec, omarchy-launch-webapp "https://chatgpt.com"
bindd = SUPER SHIFT ALT, A, Grok, exec, omarchy-launch-webapp "https://grok.com"
bindd = SUPER SHIFT, C, Calendar, exec, omarchy-launch-webapp "https://app.hey.com/calendar/weeks/"
bindd = SUPER SHIFT, E, Email, exec, omarchy-launch-webapp "https://app.hey.com"
bindd = SUPER SHIFT, Y, YouTube, exec, omarchy-launch-webapp "https://youtube.com/"
bindd = SUPER SHIFT ALT, G, WhatsApp, exec, omarchy-launch-or-focus-webapp WhatsApp "https://web.whatsapp.com/"
bindd = SUPER SHIFT CTRL, G, Google Messages, exec, omarchy-launch-or-focus-webapp "Google Messages" "https://messages.google.com/web/conversations"
bindd = SUPER SHIFT, P, Google Photos, exec, omarchy-launch-or-focus-webapp "Google Photos" "https://photos.google.com/"
bindd = SUPER SHIFT, X, X, exec, omarchy-launch-webapp "https://x.com/"
bindd = SUPER SHIFT ALT, X, X Post, exec, omarchy-launch-webapp "https://x.com/compose/post"

# Dictation
# Offline PTT (Alt+Z)
# NOTE: nerd-dictation defaults to xdotool (X11). On Wayland/Hyprland we force WTYPE.
bindd = ALT, Z, Dictation start (offline), exec, notify-send "Dictation started" && nerd-dictation begin --vosk-model-dir=/usr/share/vosk-models/small-en-us --simulate-input-tool=WTYPE --pulse-device-name=alsa_input.usb-Logitech_PRO_X_Wireless_Gaming_Headset-00.mono-fallback --sample-rate=16000 --full-sentence --numbers-as-digits
bindrd = ALT, Z, Dictation stop (offline), exec, nerd-dictation end && notify-send "Dictation stopped"

# Hybrid/high-accuracy (online Whisper when available) - records a fixed 8s clip
# Trigger on release so modifiers aren’t held while typing.
bindrd = SUPER ALT, Z, Dictation (Whisper 8s), exec, ~/.local/bin/dictate-whisper-8s

# Overwrite existing bindings, like putting Omarchy Menu on Super + Space
# unbind = SUPER, SPACE
# bindd = SUPER, SPACE, Omarchy menu, exec, omarchy-menu

# Master layout bindings
bindd = SUPER, M, Promote to master, layoutmsg, swapwithmaster master
bindd = SUPER CTRL, M, Swap with master, layoutmsg, swapwithmaster auto
bindd = SUPER, bracketright, Grow master area, layoutmsg, mfact +0.05
bindd = SUPER, bracketleft, Shrink master area, layoutmsg, mfact -0.05
bindd = SUPER, J, Focus next in stack, layoutmsg, cyclenext
bindd = SUPER, K, Focus prev in stack, layoutmsg, cycleprev
bindd = SUPER SHIFT, J, Move window down stack, layoutmsg, swapnext
bindd = SUPER SHIFT, K, Move window up stack, layoutmsg, swapprev
